clone this repository to your home directory.

type vi/vim in your command line, and press enter.

inside vim, type `:PlugInstall`, and press enter.

if your vim's version is not `9.0` or greater, try switching to branch `asyncomplete`.
